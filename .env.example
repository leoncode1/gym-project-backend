# Database connection (Spring)
DB_URL=jdbc:postgresql://localhost:5432/appdb
DB_USER=appuser
DB_PASSWORD=apppass

# Docker-only bootstrap values (used to initialize the container)
POSTGRES_DB=appdb
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppass
